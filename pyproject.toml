[project]
name = "sentry-tui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "textual>=4.0.0",
    "textual-dev>=1.7.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
